// protoc --go_out=. --go-grpc_out=. ./*.proto

syntax = "proto3";

option go_package = ".;pbrpc";

package pbrpc;

message Empty {}

message BindUserReq {
    int64 conn_id = 1;
    int64 user_id = 2;
}

service Gateway {
    rpc BindUser(BindUserReq) returns (Empty) {}
}
